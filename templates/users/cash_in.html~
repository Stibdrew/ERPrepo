{% extends 'users/base.html' %}

{% block content %}
<div class="container mt-5">
    <h2 class="mb-4 text-center text-primary">Cash In</h2>

    <!-- Success/Error Messages -->
    {% if messages %}
        <div class="mb-4">
            {% for message in messages %}
                <div class="alert alert-{{ message.tags }} text-center">{{ message }}</div>
            {% endfor %}
        </div>
    {% endif %}

    <form method="POST" action="" class="payment-form">
        {% csrf_token %}

        <!-- Amount Input -->
        <div class="form-group">
            <label for="amount">Amount to Add</label>
            {{ form.amount|add_class:"form-control shadow-sm p-3" }}
        </div>

        <!-- Credit Card Number -->
        <div class="form-group">
            <label for="card_number">Card Number</label>
            {{ form.card_number|add_class:"form-control shadow-sm p-3" }}
        </div>

        <!-- Expiry Date and CVV -->
        <div class="form-row">
            <div class="col-md-6 mb-3">
                <label for="card_expiry">Expiry Date (MM/YY)</label>
                {{ form.card_expiry|add_class:"form-control shadow-sm p-3" }}
            </div>
            <div class="col-md-6 mb-3">
                <label for="card_cvv">CVV</label>
                {{ form.card_cvv|add_class:"form-control shadow-sm p-3" }}
            </div>
        </div>

        <!-- Submit Button -->
        <button type="submit" class="btn btn-primary btn-block shadow-sm">Add Balance</button>
    </form>
</div>

<style>
    /* Style the form to look professional */
    .payment-form {
        max-width: 500px;
        margin: 0 auto;
        padding: 30px;
        border: 1px solid #e0e0e0;
        border-radius: 10px;
        background-color: #fff;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        transition: box-shadow 0.3s ease;
    }

    .payment-form:hover {
        box-shadow: 0 6px 15px rgba(0, 0, 0, 0.15);
    }

    .payment-form .form-group label {
        font-weight: 600;
        color: #333;
    }

    .payment-form .form-control {
        border: 1px solid #ced4da;
        border-radius: 6px;
        height: 50px;
        font-size: 1rem;
        transition: all 0.3s ease;
    }

    .payment-form .form-control:focus {
        border-color: #007bff;
        box-shadow: 0 0 8px rgba(0, 123, 255, 0.2);
    }

    .btn-block {
        background-color: #007bff;
        color: white;
        border: none;
        border-radius: 6px;
        height: 50px;
        font-size: 1.1rem;
        font-weight: bold;
        transition: background-color 0.3s ease, box-shadow 0.3s ease;
    }

    .btn-block:hover {
        background-color: #0056b3;
        box-shadow: 0 5px 12px rgba(0, 91, 187, 0.4);
    }

    .alert {
        border-radius: 8px;
        padding: 15px;
        font-size: 0.95rem;
        text-align: center;
    }
</style>
{% endblock %}
